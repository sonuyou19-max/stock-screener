# railway.toml — Indian Stock Screener Deployment
# Deploy this entire folder to Railway as a single project.
# Each [deploy] block is a separate service/cron job.

[build]
builder = "NIXPACKS"

# ─────────────────────────────────────────────
# Environment Variables (set in Railway dashboard)
# ─────────────────────────────────────────────
# ALERT_EMAIL_FROM       = your@gmail.com
# ALERT_EMAIL_PASSWORD   = your_16char_app_password
# ALERT_EMAIL_TO         = your@gmail.com
# BUDGET                 = 100000
